# PR Reviewer Assignment Service  
### Тестовое задание для Backend стажёра (осенняя волна 2025)

Сервис реализует автоматическое назначение ревьюверов на Pull Request’ы, управление командами и пользователями, а также переназначение ревьюверов.  
Взаимодействие происходит через HTTP API. Swagger доступен по адресу `/docs`.

---

## 📌 Основной функционал

### 👥 Управление командами
- `POST /team/add` — создание команды и её участников  
- `GET /team/get` — получение команды

### 👤 Управление пользователями
- `POST /users/setIsActive` — активация/деактивация пользователя

### 🔧 Работа с Pull Request’ами
- `POST /pullRequest/create` — создание PR и автоназначение ревьюверов  
- `POST /pullRequest/merge` — идемпотентное завершение PR  
- `POST /pullRequest/reassign` — переназначение ревьювера  
- `GET /users/getReview` — список PR, где пользователь является ревьювером  

### ❤️ Дополнительно
- `GET /health` — проверка состояния  
- Swagger UI: `/docs`

---

## ⚙️ Архитектура и принятые допущения

### 🗂 In-memory хранение
По условиям задания допускается in-memory реализация, поэтому данные хранятся в структуре:

- список пользователей  
- список команд  
- список PR  

Данные очищаются при перезапуске контейнера.

---

### 🧩 Формат ошибок
Ошибки возвращаются строго в формате OpenAPI:

```json
{
  "error": {
    "code": "ERROR_CODE",
    "message": "error description"
  }
}
