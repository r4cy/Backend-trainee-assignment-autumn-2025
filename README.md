# PR Reviewer Assignment Service  
### Тестовое задание для Backend стажёра (осенняя волна 2025)

Сервис реализует автоматическое назначение ревьюверов на Pull Request’ы, управление командами и пользователями, а также переназначение ревьюверов.  
Взаимодействие происходит через HTTP API. Swagger доступен по адресу `/docs`.

## 🧩 Формат ошибок

Ошибки полностью соответствуют OpenAPI-схеме:

```json
{
  "error": {
    "code": "ERROR_CODE",
    "message": "error description"
  }
}
```

## ⚠ Особенность OpenAPI

В примере в спецификации эндпоинта переназначения ревьювера указан параметр `old_reviewer_id`, однако в схеме OpenAPI указано поле `old_user_id`.  
Сервис использует **`old_user_id`**, так как именно он определён в спецификации.  
Это решение зафиксировано в README.

## 📄 Поведение /users/getReview

Если пользователь не найден, сервис возвращает:

```
404 NOT_FOUND
```

Так как только существующий пользователь может иметь назначенные PR.

## 🐳 Запуск через Docker

### Требования
- Docker  
- Docker Compose  

### 🚀 Запуск

```bash
docker compose up --build
```

После запуска сервис доступен по адресу:

```
http://localhost:8080
```

Проверка:

```
GET http://localhost:8080/health
```

Swagger UI:

```
GET http://localhost:8080/docs
```

## 📁 Структура проекта

```
.
├── app
│   ├── main.py
│   ├── db.py
│   └── __init__.py
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
└── README.md
```

## 📌 Принятые решения

### 1. In-memory реализация
По условиям задания такая реализация допустима.  
Фокус сделан на корректной доменной логике.

### 2. Идемпотентность merge
Эндпоинт `/pullRequest/merge` при повторном вызове возвращает текущее состояние PR без изменений.

### 3. Автоматический выбор ревьюверов
Назначаются случайные активные члены команды автора PR:
- исключая автора  
- максимум 2  
- если доступно меньше — 0 или 1  

### 4. Переназначение ревьювера
Замена происходит внутри команды заменяемого ревьювера. Условия:
- PR не MERGED  
- старый ревьювер назначен  
- есть активный кандидат  

## 🧪 Быстрый сценарий проверки

1. Создать команду → `POST /team/add`  
2. Создать PR → `POST /pullRequest/create`  
3. Проверить ревьюверов  
4. Деактивировать пользователя → `POST /users/setIsActive`  
5. Переназначить ревьювера → `POST /pullRequest/reassign`  
6. Выполнить merge → `POST /pullRequest/merge`  
7. Повторить merge → то же состояние  
8. Проверить PR пользователя → `GET /users/getReview`

